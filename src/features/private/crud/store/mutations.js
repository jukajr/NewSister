export default {
    toggleLoading(state, loading) {
        state.loading = loading;
    },
    setProjects(state, projects) {
        state.projects = projects;
    },
    setProjects2(state, projects) {
        state.projects2 = projects;
    }
};
